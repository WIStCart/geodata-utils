version: 1

solr instances:
  geodata-dev:
    url: "https://geodata-dev.shc.wisc.edu/solr/geodata-core/"
    username: "yourusername"
    password: "yourpassword"
  geodata-test:
    url: "https://geodata-test.sco.wisc.edu/solr/geodata-core/"
    username: "yourusername"
    password: "yourpassword"
  geodata-prod:
    url: "https://geodata.wisc.edu/solr/geodata-core/"
    username: "yourusername"
    password: "yourpassword"

log:
  version: 1
  disable_existing_loggers: true
  filters:
    indent:
      (): "geodatautils.logging_config.DefaultIndent"
  formatters:
    default:
      format: "%(asctime)s [%(levelname)s]%(indent)s%(message)s"
    simple:
      format: "[%(levelname)s] %(message)s"
  handlers:
    console:
      class: logging.StreamHandler
      level: WARNING
      formatter: simple
      stream: ext://sys.stdout
    file:
      class: logging.FileHandler
      level: DEBUG
      formatter: default
      filters: [indent]
      filename: geodatautils.log
      mode: w 
  loggers:
    urllib3:
      handlers: [console]
      propagate: no
  root:
      level: INFO
      handlers: [console, file]

metadata-schemas:
  geoblacklight-aardvark: config/schemas/geoblacklight-schema-aardvark.json
  geoblacklight-1: config/schemas/geoblacklight-schema-1.0.json
  geoblacklight-1-no_enum: config/schemas/geoblacklight-schema-1.0-no_enum.json